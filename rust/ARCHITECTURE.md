# AVA007 Agent Exoskeleton — Rust Execution Core

Kernel proposes capabilities; Exoskeleton/Freebuff authorizes and executes.

Endpoints: `GET /healthz`, `POST /v1/kernel/infer` on `127.0.0.1:8770`.

Package was partial in zip (main.rs only); module stubs must be completed before `cargo run`.
