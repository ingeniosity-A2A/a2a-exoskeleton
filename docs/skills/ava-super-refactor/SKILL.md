# Ava Super Refactor

Governed observe → measure → propose. Does **not** auto-merge or redefine substrate authority.

```bash
PYTHONPATH=. python3 -m refactor.super_refactor --json
PYTHONPATH=. python3 -m refactor.super_refactor --submit-agent-x
```

Canonical module: `refactor/super_refactor.py` (from ava007-super-refactoring-1.zip).
